interface:
  display_name: "SEO Code Diagnostic"
  short_description: "Run a URL-first, evidence-gated SEO audit."
  default_prompt: "Use $seo-code-diagnostic to audit this website codebase with URL-first evidence, report route coverage, and separate Confirmed findings from Candidate and Unknown evidence."
policy:
  allow_implicit_invocation: true
